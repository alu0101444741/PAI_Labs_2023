/*
  // ******************* Declarations on PokerController *******************
  private readonly PLAY_AGAIN: HTMLElement;
  private readonly STEP: HTMLElement;
  private readonly SHOW_THREE: HTMLElement;
  private readonly SHOW_FIVE: HTMLElement;
*/
/*
  // ******************* on PokerController constructor *******************
  // Replay button (refresh page)
  this.PLAY_AGAIN = document.getElementById('Refresh') as HTMLElement;
  this.PLAY_AGAIN.addEventListener('click', this.playAgain.bind(this));
  // Additional buttons
  this.STEP = document.getElementById('Step') as HTMLElement;
  this.SHOW_THREE = document.getElementById('Show3') as HTMLElement;
  this.SHOW_FIVE = document.getElementById('Show5') as HTMLElement;
*/

/** @desc Reload page so the game starts again */
/*private playAgain(): void {
  document.location.reload();
}*/

/*
  // ******************* PokerView -> cardClicked *******************
  // Reveal the clicked card
  let newCard: Card = this.DECK.popCard();
  if (card.isRevealed() && (newCard.getSuit() !== '')) {
    card = newCard;
    card.setRevealed(true);
  }
  card.setRevealed(true);*/
  /*if (!card.isRevealed())
*/
/*
<!--<div id="additionalButtons">
      <button id="Step" class="buttons"> Step </button>        
      <button id="Show3" class="buttons"> Show3 </button> 
      <button id="Show5" class="buttons"> Show5 </button> 
      <button id="PokerProbability" class="buttons"> Poker Probability </button>
    </div>  -->     
    <!--<button id="Refresh" class="buttons"> Play again </button>-->
*/